export enum UserType {
  Student,
  Staff,
}

export enum AssignStatus {
  Assigned,
  Returned,
  Lost,
  Unknown,
}

export enum UserNoteType {
  General
}

export enum UserDeviceNoteType {
  General,
  Repair,
  Warranty,
}